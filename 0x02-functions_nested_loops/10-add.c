#include "main.h"
#include <stdio.h>
/**
 * add - adds two integrers and returns the result
 *
 * @m: number being added
 *
 * @n: number being added
 *
 * Return: Always 0.
 *
 */
int add(int m, int n)
{
	return (m + n);
}
